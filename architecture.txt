spotify-recommendation-system/          ← this repository
│
├── spotify_dataset.csv                 # 32,833 Spotify tracks (public dataset, 23 columns)
├── spotify_playlist_recommendationsystem.py   # the ML pipeline — one readable script
├── web_app.py                          # 🌐 Flask website: search a song, get recommendations
├── requirements.txt                    # the 6 libraries you need
├── README.md                           # setup guide + explanation
├── .gitignore                          # keeps junk files out of git
├── LICENSE                             # MIT
└── outputs/                            # created automatically when you run
    ├── figures/
    │   ├── correlation_heatmap.png     # which audio features move together
    │   ├── elbow_method.png            # inertia per k — pick k by eye
    │   ├── silhouette_scores.png       # silhouette per k — picks k for you (--auto-k)
    │   └── song_clusters.png           # 2-D map of the song clusters
    └── cluster_summary.csv             # average feature values per cluster
