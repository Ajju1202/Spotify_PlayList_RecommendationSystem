# ============================================
# Everything this project actually uses
# (install with:  pip install -r requirements.txt)
# ============================================
pandas>=2.0          # loading & cleaning the dataset
numpy>=1.24          # numerical operations (used by pandas/sklearn)
scikit-learn>=1.3    # StandardScaler, PCA, KMeans, KNN, silhouette
matplotlib>=3.7      # charts
seaborn>=0.12        # prettier charts (heatmap, scatter)
flask>=3.0           # only needed for the web app (python web_app.py)
